defaults:
  - model: hifiplusplus
  - metrics: vctk_dataset
  - datasets: vctk_dataset
  - dataloader: example
  - transforms: example_only_instance
  - writer: cometml
  - _self_

loss_function:
  _target_: torch.nn.L1Loss

optimizer:
  _target_: torch.optim.AdamW
  lr: 3e-4

lr_scheduler:
  _target_: torch.optim.lr_scheduler.OneCycleLR
  max_lr: 1e-3
  pct_start: 0.1
  steps_per_epoch: ${trainer.epoch_len}
  epochs: ${trainer.n_epochs}
  anneal_strategy: cos

trainer:
  device_tensors: ["audio", "tg_audio"]
  device: auto
  n_epochs: 1
  epoch_len: 1
  log_step: 1
  save_period: 1
  save_dir: "saved"
  seed: 1
  override: True
  monitor: "off"
